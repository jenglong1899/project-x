The overall design should stay minimal.
A page similar to ChatGPT is enough. It does not need to copy ChatGPT exactly, and we should not add UI for features we do not have.

Chain-of-thought and tool calls should both be expanded by default.
For tool calls, the tool call and tool result should be shown in the same card so they are easier to inspect.

Tech stack: `ts + zustand store + tailwind + shadcn + zod`
