我在想是不是要把那个暂停按钮和那个发送的按钮要融合在一起啊？发送按钮点了之后，系统就在运行了，就可以显示那个暂停按钮。按了暂停按钮后，系统就会尽快暂停，这个时候暂停按钮就更换成绕圈动画，表示正在暂停。暂停下来后，就又显示回那个发送按钮，发送按钮和恢复其实等效的。

其实有个问题，你按了发送键，系统就在运行了。那么你这个时候把那个发送按钮替换成暂停按钮，但这个时候其实用户仍然是可以再发送消息的，我们系统是支持 Steer Message 的
